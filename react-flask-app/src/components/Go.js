import React from 'react';
import './CSS/Go.css';
const Go = () => {
    return (
        <div className="goButton">
          <button className="goButton" id="goButton">Begin!</button>
        </div>
    );
};

export default Go;